<Page x:Class="CSharpDex.DexList"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:CSharpDex"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Dex List">

    <!-- Main StackPanel -->
    <Grid Background="#3B3636" Height="auto" Width="auto">
        <!-- Column definitions -->
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        
        <!-- Row definitions -->
        <Grid.RowDefinitions>
            <RowDefinition Height="35"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <!-- Top border -->
        <Border Grid.Row="0" Padding="5" Height="35" Background="#4E87D4">
            <Label Name="TopLabel" Foreground="White">Pokedex</Label>
        </Border>
        
        <!-- ScrollView Dex -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Visible">
            <StackPanel Name="PokeList" Orientation="Vertical" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="10, 10, 10, 10">
                
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>
